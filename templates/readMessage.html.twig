{% extends "messages.html.twig" %}




{% block TEST %}

{% endblock TEST %}





{% block main %}

{#<div class="modal fade" id="mailViewer" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">



                        <h4 class="modal-title" id="exampleModalLongTitle">

   <img src="{{ mailContents.dirname }}/{{ mailContents.filename }}.{{ mailContents.extension }}" width="50" height="50" class="mailAvatar"> Message de {{ mailContents.username }}</h4> <p>:{{mailContents.title}}</p>

                        
                             









                    <button type="button" id="crossDismissModal" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">


                    {{ "<br>"|raw }}
                    <textarea class="form-control" name="messageContent" id="" cols="48" rows="10" disabled>{{ mailContent.message }}</textarea>
                </div>
                <div class="modal-footer">
                    <button type="button"  id="dismissModal" class="btn btn-outline-secondary justify-content-center" data-dismiss="modal">Close</button>

                </div>
            </div>
        </div>
    </div>#}

    <!-- Modal -->
    <div class="modal fade" id="mailViewer" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">  <img src="{{ mailContents.dirname }}/{{ mailContents.filename }}.{{ mailContents.extension }}" width="50" height="50" class="mailAvatar"> Message de {{ mailContents.username }}</h4>      </h5>
                    <button type="button" class="close" id="crossDismissModal" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    {{ mailContents.message }}
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-success">repondre</button>
                    <button type="button" id="dismissModal" class="btn btn-outline-secondary" data-dismiss="modal">Close</button>

                </div>
            </div>
        </div>
    </div>




{% endblock main %}

{% block script %}
    {{ parent() }}

    <script>  $('#mailViewer').modal('show');</script>

    <script> document.getElementById('dismissModal').addEventListener('click',function () {
            window.location.href="index.php?p=messages"
        });</script>
    <script> document.getElementById('crossDismissModal').addEventListener('click',function () {
            window.location.href="index.php?p=messages"
        });</script>

{% endblock script %}
